<div class='container px-5 px-sm-0'>
  <div class='row'>
    <div class='col-md-3'>
      <h2>User info</h2>
      <%= render 'info', user: @user %>
      <h2 class="mt-3">New book</h2>
      <%= render 'books/form', book: @book %>
    </div>
    <div class='col-md-8 offset-md-1'>
      <h2>Books</h2>
      <%= render 'books/index',books: @books %>
    </div>
  </div>
    <h1><%= @user.name %></h1>
      <p>今日の投稿数: <%= @today_count %></p>
      <p>昨日の投稿数: <%= @yesterday_count %></p>
      <p>前日との差: <%= @difference_percentage %> %</p>
      <p>今週の投稿数: <%= @this_week_count %></p>
      <p>先週の投稿数: <%= @last_week_count %></p>
      <p>今週と先週の差: <%= @week_difference %></p>
      
      <!-- 過去7日間の投稿数一覧 -->
      <h2>過去7日間の投稿数</h2>
      <ul>
        <% if @past_seven_days_counts.present? %>
          <h2>過去7日間の投稿数</h2>
          <ul>
            <% @past_seven_days_counts.each do |date, count| %>
              <li><%= date %> : <%= count %></li>
            <% end %>
          </ul>
        <% else %>
          <p>過去7日間の投稿はありません。</p>
        <% end %>
      </ul>
      
      <!-- chart.jsを使ったグラフの表示 -->
      <%= javascript_pack_tag 'chart' %>
      <canvas id="postChart"></canvas>
      
      <script>
        // グラフの表示ロジックを書く（chart.jsの使い方に従って）
      </script>
</div>
